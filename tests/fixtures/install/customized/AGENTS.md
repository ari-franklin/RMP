# Repository instructions

## Roadmap maintenance

Follow [the repository protocol](./docs/roadmap-protocol.md). At the start of every task, run `npx rmp sync`, then read `ROADMAP.md` before planning or editing. Before the final response, run `npx rmp sync` again after meaningful work. Stay quiet on no-op maintenance; mention only material roadmap changes, decisions needed, or unresolved failures.
